ascii_values = dict((value, ord(value)) for value in input().split(', '))

print(ascii_values)

# ascii_values = {value: ord(value) for value in input().split(', ')}
#
# print(ascii_values)

# ascii_values = {}
#
# data = input().split(', ')
#
# for value in data:
#     ascii_values[value] = ord(value)
#
# print(ascii_values)
